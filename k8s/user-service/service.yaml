apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devops-app
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8000
